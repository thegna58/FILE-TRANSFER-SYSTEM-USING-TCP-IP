# FILE-TRANSFER-SYSTEM-USING-TCP-IP
This project demonstrates file transfer over a network using the TCP/IP protocol with Python. It implements a simple client-server architecture where the server hosts files, and the client requests and receives them. The project showcases the use of Python's socket module for network communication.
